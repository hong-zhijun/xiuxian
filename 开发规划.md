# 修仙宗门文字游戏开发规划

详细规划已放在 `开发规划/`，本轮未开发应用代码，原始 `基础需求.md` 保持不变。

## 技术栈修订

2026-09-17按明确的Cloudflare目标修订：**Vue 3 + TypeScript + Vite；Cloudflare Workers + Hono；D1（SQLite）；Wrangler本地开发和部署**。前后端分离部署：**前端 → Cloudflare Pages，后端 → Cloudflare Workers**，通过同域 Route 连接，无需 CORS。

不需要Docker、PostgreSQL或独立Node服务器。本地运行Wrangler的D1模拟环境，线上使用D1托管数据库；不是上传一个普通.sqlite文件供Worker修改。事务、迁移、测试、定时任务与部署规则已同步改为D1/Workers方案，详见02文档。

## 建议分期

| 期数 | 范围 | 任务数 |
| --- | --- | --- |
| P0 | 工程、账号、事务、测试 | 7 |
| P1 | 宗门、弟子、挂机、升级、突破 | 9 |
| P2 | 功法、炼制、历练、自动战斗 | 9 |
| P3 | 弟子关系、人生、事件、史册 | 7 |
| P4 | 档案、拜访、切磋、排行榜 | 8 |
| P5 | 玩家坊市、托管、交易审计 | 6 |
| P6 | 世界妖兽、灵兽、上线加固 | 8 |

共54个可验收任务。P1交付最小可玩版，P3验证内容趣味，P6交付完整V1.0；每期评审后再继续。

## 从哪里开始

先读 `开发规划/00-阅读顺序与执行入口.md`，确认其中五项产品决策；再按 `开发规划/06-弱模型执行说明与任务模板.md`，一次让模型执行一个任务。

首次执行 `P0-01`。各阶段的任务文件在 `开发规划/分期任务/`，状态记录在 `开发规划/07-进度与决策记录.md`。

完整材料包括范围追踪、技术架构、数据库与数值规则、接口/页面、测试与上线门禁、执行提示词、分期任务和进度表。

注意：建议将自由留言、寿元死亡、渡劫死亡、叛离延期V1.1。这些是待确认范围变更，不是已经完成或被取消的需求。
