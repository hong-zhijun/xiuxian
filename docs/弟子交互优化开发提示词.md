# 弟子交互优化实施提示词

请在 `C:\Users\lingshi\.codex\worktrees\disciple-interaction\xiuxian-game`（分支 `codex/disciple-interaction`）中，严格按 `docs/弟子交互优化开发计划.md` 完成弟子管理交互优化。先完整阅读计划及相关代码，不要只做视觉稿；完成后留下未提交 diff，报告测试与视口检查的真实结果，不自动提交或推送。

**先核对基线**：运行 `git status -sb` 并确认当前目录在独立 worktree。该分支起自 `225e44a`；主工作区有另一个 agent 正在做 `0016` 六属性/评分。只有对方已提交后才同步其提交，合并冲突时保留双方功能；不要读取主工作区未提交文件后直接复制覆盖，不要修改主工作区。迁移预留 `0017_disciple_avatar_frame.sql`，若被占用取下个编号。若 0016 尚未可集成，先实现不依赖它的部分并报告待集成，不要把旧四轴雷达覆盖到新六轴上。

**逐项实现**：

1. 名册修为数值常显。达到当前阶段修为门槛且不是版本上限时，头像变成真正的快捷破境按钮；点击先开确认弹窗，展示服务端胜算、灵气消耗、阻止原因及现有规则的非定量失败风险说明。当前接口没有失败后果字段，不要复制公式或凭空加字段。不可破境可看原因但不能确认。取消/Esc/遮罩不能发请求；确认才复用 `/game/breakthrough`，防重复并用完整 `state` 更新。不能用本地动画或四舍五入的 100% 当执行资格；不能嵌套两个交互控件；普通头像不是按钮，详情入口保留。
2. 详情四 Tab：默认「概览」必须有雷达、精确属性、战力/天赋及简短摘要；「修行」放修为/伤势、岗位、破境和服药；「历练」放预览、在外、领取及当前弟子的近期记录；「档案」放头像框、备注和驱逐。紧凑且窄屏可滚动，切换不丢未保存备注或历练选择；使用可键盘操作的 tablist/tab/tabpanel。`state.journey.recent` 是全宗最近十条，必须按 `discipleId` 筛选，只能称近期记录。二级服药弹窗保持正确层级、Esc 和焦点回退。
3. 已有境界筛选，不要重复造；增加从属的境界内阶段筛选，换境界清阶段。新增计划中的修为进度分档，用服务端原始比值和 `canBreakthrough`，不用本地平滑值。增加计划所列高低排序，稳定处理并列；保留已合入的评分**排序**，但不做评分**筛选**。先在 `apps/web/src/utils/discipleFilter.ts` 做纯函数和 `tests/web/disciple-filter.test.ts` 测试，再接 `DiscipleRoster.vue`。
4. 头像框是用户提供的十张固定静态图，ID `frame01`–`frame10`，另有旧样式 `classic`；不是自由上传。当前只有一张示例，不要伪造九张或复制示例。素材齐备后做默认值和白名单迁移、`DiscipleRow` 全部 SELECT、私有视图、严格 schema、仓储单列写回、service 归属/快照守卫、`POST /api/v1/game/set-disciple-avatar-frame`、前端 API 和档案图像单选器。名册最外修为环、内侧框图和中央首字；图片失败回退旧样式。旧/新弟子默认 `classic`，公开档案/战报/榜单不泄露。素材缺失可以完成独立部分，但必须明确头像框未验收。

**关键路径**：`apps/web/src/components/{DiscipleRoster,DiscipleDetailDialog,SectScreen,ModalShell}.vue`、`apps/web/src/utils/discipleFilter.ts`、`apps/web/src/styles/base.css`、`apps/web/src/{App.vue,api/game.ts}`；后端 `apps/server/src/modules/game/{schema,routes,service,repository,view}.ts`、`migrations/`；测试 `tests/web/disciple-filter.test.ts`、`apps/server/test/disciple-management.test.ts`。沿用现有 API envelope、鉴权/CSRF、Toast、ModalShell、服务端权威状态和组件事件链，不引新路由/状态库/图表框架，不改玩法公式。

**交付前**：执行 `npm run test:unit`、`npm run typecheck`、`npm run build`、`git diff --check`；补 Worker/D1 的跨宗、非法框 ID、持久化、迁移和隐私测试。可用浏览器时检查桌面、320/375/680px、键盘 Tab/方向键/Esc、触屏头像、长文案和图片加载；不能跑则说明原因。报告改动文件、迁移与接口、素材到齐情况、每条命令的结果、未完成项及 0016 合并情况。不得把缺失素材或未执行的视觉验收说成完成。
